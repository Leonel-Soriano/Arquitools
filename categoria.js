const list=window.ARQUITOOLS.filter(t=>t.categoria==='laboratorio'&&t.grupo==='bouncing-balls');document.getElementById('count').textContent=list.length+' DISPONIBLE'+(list.length===1?'':'S');const container=document.getElementById('simulators');for(const t of list){const a=document.createElement('a');a.className='tool';a.href=t.url;const icon=document.createElement('span');icon.className='tool-icon';icon.textContent=t.icono;icon.setAttribute('aria-hidden','true');const body=document.createElement('div'),h=document.createElement('h3'),p=document.createElement('p');h.textContent=t.titulo;p.textContent=t.detalle;body.append(h,p);const badge=document.createElement('span');badge.className='badge available';badge.textContent='Abrir ↗';a.append(icon,body,badge);container.append(a);}